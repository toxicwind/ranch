# herd

The local inference front door. Lives in the monorepo:

**`sovereign-projects/projects/herd`** — llama-swap fork (Go), `:25100`.

This directory is a pointer; the source of truth is the monorepo.
