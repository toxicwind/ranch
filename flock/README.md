# flock

The external cloud provider router. Own repo, checked out in projects:

**`/home/toxic/projects/flock`** → [`toxicwind/flock`](https://github.com/toxicwind/flock)
— Rust proxy, `:25193`.

This directory is a pointer; the source of truth is `toxicwind/flock`.
